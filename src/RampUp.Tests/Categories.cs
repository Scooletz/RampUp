namespace RampUp.Tests
{
    public static class Categories
    {
        /// <summary>
        /// CodeCop based tests are excluded by default (see RampUp.sln.DotSettings file) as they conflict with other tests using <see cref="RampUp.Atomics"/>.
        /// </summary>
        public const string CodeCop = "CodeCop";
    }
}